apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tvm.fullname" . }}-test-connection"
  labels:
    {{- include "tvm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tvm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
