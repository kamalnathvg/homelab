apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chandiroor.fullname" . }}-test-connection"
  labels:
    {{- include "chandiroor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chandiroor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
