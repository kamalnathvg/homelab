apiVersion: v1
kind: Service
metadata:
  name: {{ include "chandiroor.fullname" . }}
  labels:
    {{- include "chandiroor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chandiroor.selectorLabels" . | nindent 4 }}
