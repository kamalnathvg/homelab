apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: chandiroor
spec:
  chart:
    spec:
      sourceRef:
        kind: GitRepository
        name: santhigiri-dev
  values:
    env:
      APP_ENV: development
    image:
      tag: dev-2026.08.04.001
